# archive/

Código muerto. Se conserva solo como referencia histórica.

`index2.html` es la iteración anterior del sitio, de cuando se llamaba VOOV. Los `.js`
sueltos (`_chk_scn.js`, `_motioncheck.js`, `_synctest.js`, `sim-data-verify.js`,
`zz_realscn.js`, `zz_simjs.js`, `zzfresh1.js`) son scripts de verificación de esa época.

Nada de esta carpeta está enlazado desde el sitio en vivo, y **las rutas que contiene ya
están rotas**: apuntan a `assets/styles.css` y `assets/site.js`, que hoy viven en
`assets/css/` y `assets/js/`.

No uses estos archivos como punto de partida. El sitio actual está en la raíz del repo.
